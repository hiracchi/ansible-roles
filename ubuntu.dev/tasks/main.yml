- name: install devtools
  become: yes
  apt: 
    pkg:
      - build-essential
      - clang
      - llvm
      - gfortran

      - make
      - automake
      - autoconf
      - libtool
      - pkg-config
      - cmake

      - flex 
      - bison 
      - patch
      - bc

      - git
      - git-lfs

      - libbz2-dev

      # python
      - python3-dev

    state: latest

